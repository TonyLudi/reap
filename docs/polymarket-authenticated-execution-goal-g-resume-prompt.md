# Goal G Return Sequence: G-R Amendment 6 Through Goal G Completion

Status: **runnable after the shared authorization package is committed**.

## Runnable Goal Prompt

> /goal Execute the Goal G return sequence exactly as specified in
> `docs/polymarket-authenticated-execution-goal-g-resume-prompt.md`. First
> validate and close the prospective defect-class repair under Goal G-R
> Amendment 6. If and only if it is green, activate Goal G Amendment 3,
> establish a fresh current-revision Phase 0 baseline and replay in a distinct
> evidence root, and then continue through the original Goal G Phases 1-7.
> Preserve every historical red result and stop only at Goal G completion or a
> documented stop condition.

## Objective

Return safely from Goal G's stopped replay evidence to the original bounded
authenticated Polymarket-connectivity objective without:

- relabeling or retrying the historical Goal G attempt;
- manufacturing the missing historical Goal G-R primary variants;
- weakening a semantic, durability, resource, security, or capability gate;
- using a stale PM action baseline after the evidence workload changed; or
- broadening Goal G's product scope or production authority.

The execution has four sequential stages:

```text
Stage R6  validate Amendment 5 and close only the defect-class repair
    |
Stage G3  activate Goal G Amendment 3 from the R6 completion commit
    |
Stage P0  establish fresh sources/inventories/confidence/baseline/replay
    |
Stages 1-7  execute the original Goal G implementation and global gates
```

Continue automatically only across green gates.

## Controlling Documents

Apply documents in this precedence order:

1. this return-sequence prompt;
2. `docs/polymarket-authenticated-execution-goal-g-amendment-3.md`;
3. `docs/polymarket-authenticated-execution-goal-g-amendment-3-runner-contract.md`;
4. `docs/goal-g-replay-repair-amendment-6.md`;
5. `docs/goal-g-replay-repair-prompt.md` for historical facts and any exact
   stable command form expressly imported by Amendment 3;
6. Goal G Amendments 1-2 in
   `docs/polymarket-authenticated-execution-goal-g-prompt.md`;
7. the remaining original Goal G prompt and
   `docs/polymarket-authenticated-execution-boundary.md`;
8. the Goal G and Goal G-R handoffs as immutable history plus append-only
   execution records.

Amendment 6's prospective defect-class completion meaning and fixed
ten-command post-stop campaign replace the incompatible original
historical-equivalence completion/campaign for closure; the original
historical investigation remains stopped. Amendment 3 and its runner
contract replace the old Phase 0 activation, evidence/runtime/recorder,
source re-attestation, confidence, current-baseline, replay, storage,
contamination/no-retry, and completion/return procedures. Every other
original requirement remains.

## Starting Gate

Start on clean `master` at the shared authorization commit:

```text
subject=docs: authorize goal g-r closure and conditional goal g return
parent=77ad6f30f79eb0b6d99881da97ec94e550364d1a
```

Call that exact eight-document commit `A`; require the following
direct-child chain with no intervening commit:

```text
A -> R6 -> G3 -> P0
```

The tracked deltas are exact: `A` is the eight-path package listed by
Amendment 6; `R6` changes only the Goal G-R handoff; `G3` changes only the
Goal G handoff; and `P0` changes only the Goal G handoff.

Require:

- local `origin/master` relation recorded without fetch;
- frozen `Cargo.lock` SHA-256
  `2673d055c943c3bd5444531b67df280026c145cbbbc99b68a06f4ac0c2dbb0ff`;
- exact Amendment 5 implementation ancestry and two-file delta;
- all original Goal G and Goal G-R evidence hashes;
- clean `../imm-strategy` at its frozen revision;
- pinned Predarb object availability and only its known dirty path names;
- no overlapping process; and
- at least `2,147,483,648` repository bytes.

Any mismatch is a stop.

Immediately before every ignored evidence creation, redirected log, Cargo
command, tracked edit, and commit, run the exact shared storage preflight from
Amendments 6 and 3. Use only their absolute repository-owned runtime roots.

## Stage R6: Goal G-R Amendment 6

Execute `docs/goal-g-replay-repair-amendment-6.md` exactly:

1. authenticate repository, implementation, prior evidence, process, runtime,
   and storage state;
2. create the new Amendment 6 evidence root and closed ten-label recorder;
3. run and retain its static pre-Cargo reviews;
4. execute each fixed command once in order;
5. validate exact test cardinality, combined report and projection, Goal F
   anchors, clean pre/post identity, process isolation, and evidence hashes;
6. seal the campaign; and
7. append and commit the Amendment 6 completion handoff.

Do not revive v5, create its missing regression runner, or run the original
Goal G Phase 0 helper.

If any R6 command or validator is red, stop. Do not activate Goal G Amendment
3.

## Stage G3: Amendment 3 Activation

Only from the clean Amendment 6 completion commit:

1. re-authenticate every prerequisite in Amendment 3;
2. prove the bundle, evidence, and runtime paths are absent;
3. create, no-Cargo self-test, statically review, hash, and seal only the
   pre-activation recorder bundle;
4. append exact Goal G-R completion and bundle identities to the Goal G
   handoff and mark Amendment 3 active; and
5. commit that handoff alone with exact subject
   `docs: activate goal g amendment 3`.

No source change or evidence/runtime-root creation is authorized during
activation.

## Stage P0: Fresh Goal G Phase 0

Execute Amendment 3 Phases 0A-0D on the clean activation revision:

1. verify the activation-sealed bundle, create the evidence/runtime roots
   once, and run identity, frozen source re-attestation, inventories,
   siblings, and anchors;
2. the complete repeated defect-class confidence campaign;
3. the new 16-invocation engine/live/Chaos-action/PM-action baseline and its
   schema/non-timing bridge against the immutable old baseline (old timing is
   diagnostic only);
4. one fresh immutable Goal G replay in the new root; and
5. a complete Goal G handoff record.

The original Goal G root remains untouched and red.

A valid red result in any new campaign is retained and stops the sequence. It
is not contamination and cannot be retried on the unchanged activation
revision.

When every Phase 0 gate is green, commit:

```text
docs: qualify goal g amendment 3 phase 0
```

That documentation-only commit updates only the Goal G handoff to Phase 0
green/Phase 1 ready.

## Stages 1-7: Original Goal G

After the Phase 0 gate commit, continue automatically through the original
Goal G phases:

1. backend-neutral prepared effects and exact fake parity;
2. secret custody types, L2 authentication, and EOA signing using only
   immutable public vectors and canary credentials;
3. public, finalized-chain, authenticated read-only, and published-position
   owner-local loopback roles;
4. authenticated journal first, then exact live place-GTC-post-only and
   exact-owned cancel;
5. static product, bounded recovery, reconciliation, cleanup, and shutdown;
6. credential-free fault, security, canary, determinism, allocation, memory,
   and local performance evidence; and
7. documentation plus all workspace/release/systemd/audit/metadata/global
   gates.

Use the new Phase 0 baseline for final performance comparison. Preserve all
Goal D/F/Chaos artifacts and semantics. Split `workload.rs` mechanically
before any later semantic change: leave it below `1,400` lines, every new
production file at or below `1,000` lines, and functions within the original
`200`/`250`-line limits.

Commit each original phase only when its focused gate is green. Do not push
during the goal unless the user separately authorizes that later push.

## Completion

The return sequence completes only when:

- Amendment 6 defect-class validation is durably green while its historical
  contract remains honestly stopped;
- Goal G Amendment 3 Phase 0 is green under its new root and current baseline;
- original Goal G Phases 1-7 and every focused/global gate pass;
- all Goal D/F/Chaos and historical Goal G/G-R evidence is exact;
- every required canary scan is clean;
- no real credential, authenticated external request, real Polygon request,
  or real order was used;
- Reap and `../imm-strategy` are clean and Predarb's known dirty paths remain
  unchanged and unread; and
- the final handoffs record exact revisions, commands, hashes, evidence,
  limitations, and open deployment/economic/account/host/trial gates.

Completion has the original Goal G meaning: production-shaped, narrowly
authenticated Polymarket connectivity as a sealed library composition. It
does not mean production deployment, economic validity, account
qualification, target-host qualification, live-order testing, or trading
approval.

## Stop Conditions

Stop on any condition named by Amendment 6, Amendment 3, or the original Goal
G prompt, including:

- a historical or current evidence mismatch;
- an unexplained worktree/sibling/process state;
- storage below the exact floor;
- a recorder/validator defect after execution begins;
- any clean command or semantic gate failure;
- a need to retry, discard, serialize, sleep, expand a timeout, reduce a
  workload, weaken an assertion, regenerate a golden, or grow an unauthorized
  allowlist;
- protocol ambiguity that cannot fail closed within the reviewed boundary;
- a need for broader connectivity, arbitrary requests/RPC, credentials,
  production traffic, real orders, target-host tuning, or an economic model;
  or
- any source/file growth outside the phase's reviewed authority.

At a stop, preserve all evidence, record the exact blocker, and do not
continue to the next stage.

```text
production_order_entry_authorized=false
real_credentials_loaded=false
authenticated_external_request_sent=false
real_polygon_rpc_request_sent=false
real_order_submitted=false
```
